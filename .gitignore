# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache

# next.js
.next/
out/
next-env.d.ts

# production
build/

# misc
.DS_Store
*.pem
*.log

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (keep these out of git)
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# worklog (billable time, local-only)
worklog/sessions.jsonl
worklog/worklog_report.xlsx

# source data (large, contains real names)
source_data.xlsx

# misc
.Rhistory

# husky internals (regenerated on `npm install` via prepare script)
.husky/_/

# Claude Code local settings (per-machine preferences, not project config)
.claude/settings.local.json
.claude/projects/

# excel lock files
~$*.xlsx

# Magna internal build orchestration (not a customer deliverable)
/build/

# worklog rotation backups
worklog/*.bak.*
