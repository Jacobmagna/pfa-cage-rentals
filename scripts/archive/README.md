# Archived one-shot scripts

Historical one-shot scripts. Already executed against production. Don't re-run without confirming with Jacob — they mutate prod (`.env.local` `DATABASE_URL` points at the Neon prod branch). Kept here as evidence of what ran during the historical-import / rate-snapshot rollout in May 2026.
