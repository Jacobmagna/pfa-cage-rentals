"use server";

// Coach-side profile actions intentionally live in src/app/actions.ts
// (see updateOwnName) — that path covers both coach and admin renames
// and is already wired into the editable-name component. This file
// is reserved for coach-only profile actions if/when they appear.
